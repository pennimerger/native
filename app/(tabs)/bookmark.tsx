import { View, Text } from 'react-native'
import React from 'react'

const Bookmark = () => {
  return (
    <View>
      <Text>Bookmark</Text>
      {/* It is one thing to suspend a service and another 
      to invalidate the entire projectId */}
      {/* Never use appwrite for your backend guys */}
      {/* I'll be back to this. */}
    </View>
  )
}

export default Bookmark